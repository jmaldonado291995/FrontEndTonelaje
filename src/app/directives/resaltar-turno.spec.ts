import { ResaltarTurno } from './resaltar-turno';

describe('ResaltarTurno', () => {
  it('should create an instance', () => {
    const directive = new ResaltarTurno();
    expect(directive).toBeTruthy();
  });
});
