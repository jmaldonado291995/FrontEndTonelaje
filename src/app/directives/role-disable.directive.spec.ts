import { RoleDisableDirective } from './role-disable.directive';

describe('RoleDisable', () => {
  it('should create an instance', () => {
    const directive = new RoleDisableDirective();
    expect(directive).toBeTruthy();
  });
});
